Enable DMKS building of these modules:

VER=1.16.4-k612
sudo mkdir -p /usr/src/morse-$VER
sudo git clone --recursive https://github.com/EtherFidelity/morse_driver_kernel_612.git /usr/src/morse-$VER
sudo dkms add -m morse -v $VER
sudo dkms build -m morse -v $VER
sudo dkms install -m morse -v $VER

You'll still need the firmware:

sudo mkdir -p /lib/firmware/morse
cd /lib/firmware/morse
sudo wget https://github.com/MorseMicro/firmware_binaries/releases/download/v1.15.3/morsemicro-fw-rel_1_15_3_2025_Apr_16.tar
sudo tar -xf morsemicro-fw-rel_1_15_3_2025_Apr_16.tar -C /
sudo rm morsemicro-fw-rel_1_15_3_2025_Apr_16.tar

